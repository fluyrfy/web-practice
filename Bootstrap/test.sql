
SELECT ${*} FROM `` WHERE salary=SELECT MAX(salary) FROM `emp`

for(let i in emp) {
  if (i.dep == a) {
    x + = i.salary
  } elif (i.dep ==b ) {
    y += i.salary
  } else {
    z += i.salary
  }
}
